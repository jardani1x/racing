; Phase 0 baseline engine config.
; Values follow the shipped UE 5.8 TP_Blank template unless noted.
; Rendering-quality decisions (Nanite, Lumen, VSM, TSR) are deliberately NOT set
; here: CLAUDE.md requires they be enabled only after measurement on the
; reference GPU worker, which does not exist yet (BLOCKER-001, ADR-0003).

[/Script/EngineSettings.GameMapsSettings]
GameDefaultMap=/Engine/Maps/Templates/OpenWorld
EditorStartupMap=/Engine/Maps/Templates/OpenWorld

[/Script/Engine.RendererSettings]
r.AllowStaticLighting=False

[/Script/WindowsTargetPlatform.WindowsTargetSettings]
DefaultGraphicsRHI=DefaultGraphicsRHI_DX12
-D3D12TargetedShaderFormats=PCD3D_SM5
+D3D12TargetedShaderFormats=PCD3D_SM6
-D3D11TargetedShaderFormats=PCD3D_SM5
+D3D11TargetedShaderFormats=PCD3D_SM5
Compiler=Default
AudioSampleRate=48000
AudioCallbackBufferFrameSize=1024
AudioNumBuffersToEnqueue=1
AudioMaxChannels=0
AudioNumSourceWorkers=4

; ---------------------------------------------------------------------------
; CONFIG HYGIENE
;
; Do not commit an [/Script/AndroidFileServerEditor.AndroidFileServerRuntimeSettings]
; block here. The editor regenerates it on launch with a fresh secret-shaped
; SecurityToken and bAllowNetworkConnection=True. It is inert on this project
; (plugin not enabled, bIncludeInShipping=False, Android is not a target), but it
; dirties a tracked file on every editor run and puts a rotating credential-shaped
; value into git history. Two such tokens are already in history at d9ac6c3 and
; 8efbfd5; they are not secrets in any meaningful sense, so they are left rather
; than rewriting history. Delete the block if it reappears -- do not rotate it.
; Tracked under SEC-001. Caught by code-reviewer, 2026-08-10.
; ---------------------------------------------------------------------------

